/** Horizontal padding shared by screen body and stack header (keeps title aligned with content). */
export const SCREEN_HORIZONTAL_PADDING = 20;

/** Max content width on web — matches `ScreenBody` constraint. */
export const WEB_MAX_CONTENT_WIDTH = 480;
